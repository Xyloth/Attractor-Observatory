docs/DOCTRINE.md text eol=lf
docs/doctrine_d*.md text eol=lf
docs/doctrine_registry.json text eol=lf
spec/lineage.json text eol=lf
spec/CHANGELOG.md text eol=lf
papers/methods/*.md text eol=lf
papers/prereg/**/*.signed.json text eol=lf
papers/methods/INGESTION_TARGETS.md text eol=lf
INGESTION_TARGETS.md text eol=lf
# CB-018: spec lineage source files. lineage.json's recorded sha256 was
# computed against LF bytes; without these rules, fresh Windows worktrees
# pick up CRLF and test_spec_lineage_hashes_match_raw_bytes fails.
# git's .gitattributes parser doesn't accept spaces in patterns (even with
# escapes / character classes per Windows git build), so the spec files
# below are kept LF in-repo and rely on the per-commit canonical bytes
# matching lineage.json's recorded sha256. Architect escalation candidate:
# rename spec sources to spaceless paths in a future spec-vN.M boundary.
